# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
Pillow>=9.0.0
PyYAML>=6.0
tqdm>=4.65.0

# Optional but recommended
tensorboard>=2.13.0     # For TensorBoard logging
wandb>=0.15.0           # For Weights & Biases logging

# FID evaluation (choose one)
clean-fid>=0.1.35       # Recommended: pip install clean-fid
# torch-fidelity>=0.3.0 # Alternative

# Development
matplotlib>=3.7.0
ipython>=8.0.0
jupyter>=1.0.0
